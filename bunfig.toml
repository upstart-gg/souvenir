[test]
# ISO Compliance: Use dbmate migrations for test database setup
# env.ts setup the DATABASE_URL before tests run
preload = ["./src/__tests__/env.ts", "./src/__tests__/setup.ts"]
# Timeout for each test (30 seconds)
timeout = 30000
